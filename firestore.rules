rules_version = '2';
// NOTE: These rules allow full public read/write access.
// Before going to production, restrict access using Firebase Authentication,
// e.g.: allow read, write: if request.auth != null;
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
